# 2026-07-30 開發紀錄（第二次更新）

## 本次異動
- 重寫 `config/projects.json`：改為 6 筆真實作品（生日網頁作品集入口卡片、Ya3s 線上商城系統、鄒老師病人衛教卡系統、工廠薪水計算系統、洗衣店代收款記錄系統、家庭聚餐紀錄網頁）
- 新增 `config/birthday-links.json`，收錄 18 筆生日網頁連結，並依「家人／朋友／老師／延伸內容」分組
- 新增子頁面 `works/birthday/index.html`，作為生日網頁作品集的完整清單頁
- 將導覽列、頁尾等首頁與子頁面「共用」的渲染邏輯抽出至 `js/shared-render.js`，避免同樣的程式碼在兩個進入點檔案（`main.js`、`birthday-page.js`）重複撰寫
- `config/projects.json` 每筆改為支援最多兩個連結（`link` / `link2`），對應 Ya3s（前台＋後台）與鄒老師衛教卡（系統＋使用說明）的雙連結需求
- 補上 Ya3s 會員資料庫細節：會員資料存放於 Firebase Firestore 的 `user` 資料庫
- 新增 `netlify.toml`（純靜態網站部署設定）與 `.gitignore`，並在 README 補上上傳 GitHub 與部署 Netlify 的操作步驟
- 修飾所有作品的說明文字，並逐筆核對內容是否符合使用者實際提供的功能描述

## 待辦事項
- 填入真實姓名、聯絡方式
- 放入個人照片至 `assets/images/profile-photo.jpg`
- 證照證書清單與縮圖（尚待使用者提供發證單位、名稱、日期與圖檔）
