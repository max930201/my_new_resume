# 2026-07-30 開發紀錄（第三次更新）

## 本次異動
- 新增「證照證書」區塊：`config/certificates.json`（含 2 筆佔位範例）、`css/certificates.css`、`index.html` 新增區塊、`js/main.js` 新增 `renderCertificates()`
- 新增 `config/nav.json` 的「證照證書」導覽項目
- 新增 `assets/images/certs/` 資料夾與使用說明
- 將照片框（`.photo-frame`）、作品卡片（`.project-spec`）重複的「藍圖角標」裝飾樣式，抽成 `css/base.css` 的共用類別 `.corner-marks` / `.corner-marks--accent`，證書卡片也套用同一份，避免三處各自重複撰寫同樣的 CSS
- 更新 README 的專案結構圖與客製化說明

## 待辦事項
- 填入真實姓名、聯絡方式
- 放入個人照片
- 填入真實證照資料與縮圖（使用者確認之後會自行處理）

## 追加調整（同日）
- 學習歷程改為垂直時間軸設計：漸層連接線、節點標記、最後一筆自動標示「現在」
- 區塊底色改為交錯呈現，並新增 `--color-paper-alt` token，避免白色卡片（作品卡、證書卡、聯絡方式）與區塊背景同色而失去對比
- `.section-title` 加上識別線裝飾
- 作品卡片、證書卡片加上滑過浮起的互動回饋
- 清理 `responsive.css` 內已淘汰的舊時間軸格線規則
